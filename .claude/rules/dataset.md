---
paths:
  - 'packages/species-data/**'
---

Dataset files follow the `kb-dataset` skill: stable slugs, four-locale names and notes, ≥ 1 cited
source per rule, validator green, idempotent seed. Agent: `kb-curator`.
