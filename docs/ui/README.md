# UI specs

One file per surface, `<N.m>-<surface>.md`, written by the `ui-designer` agent before the surface
is built (format in `.claude/agents/ui-designer.md`). The design system itself lives in
`wiki/ui-design-system.md`.
