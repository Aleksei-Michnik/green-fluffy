# Learnings log

Append-only. Written by the `learn` skill: one row per finding, newest last. `Applied to` names
the agent / skill / wiki page / index that was updated so the lesson sticks.

| Date       | Finding                                                                                                                                                                                                                                         | Evidence                                                                                               | Applied to                                                                                           |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| 2026-09-24 | Per-project Haraka is superseded by the shared relay with client-side DKIM                                                                                                                                                                      | infra `mail/README.md`                                                                                 | `wiki/infra-context.md`, `decisions.md`                                                              |
| 2026-09-24 | Production deploys must be dispatch-only; ported `push: [main]` trigger is wrong for 0.8                                                                                                                                                        | infra `next-session.md` §2                                                                             | `wiki/infra-context.md`, `devops` agent                                                              |
| 2026-09-24 | Mdock registry already contains green-fluffy; adoption is a small compose change in this repo                                                                                                                                                   | `infra/mdock/hosts.json`                                                                               | `wiki/infra-context.md`, `devops` agent                                                              |
| 2026-09-24 | This repo's commits carry the Claude co-author trailer; myfinpro's rules differ                                                                                                                                                                 | `git log`                                                                                              | `commit-hygiene` skill                                                                               |
| 2026-09-24 | Clone's git identity (global, organisation) differs from the repo history (personal)                                                                                                                                                            | `git config user.email` vs `git log`                                                                   | `wiki/gotchas.md`, `commit-hygiene` skill                                                            |
| 2026-09-24 | Mdock adoption is network membership only; routers are generated (websecure only), never labels                                                                                                                                                 | infra `mdock/traefik/docker-compose.yml`, sibling session                                              | `wiki/infra-context.md`, `local-stack` skill                                                         |
| 2026-09-24 | No scheduled backups exist on the shared server for any project; 0.9 must not assume them                                                                                                                                                       | infra `docs/13-deploy-runbook.md` §5                                                                   | `wiki/infra-context.md`, `wiki/deployment.md`                                                        |
| 2026-09-25 | Hot reload behind mdock verified end to end; 403 without `MDOCK_DEV_ORIGINS`, 200 with; HMR 101                                                                                                                                                 | `wiki/deployment.md` table                                                                             | `wiki/deployment.md`, `decisions.md`                                                                 |
| 2026-09-25 | Rebuilt dev images still fail until anonymous volumes are renewed (`up -d -V`)                                                                                                                                                                  | local stack, this session                                                                              | `wiki/gotchas.md`, `local-stack` skill                                                               |
| 2026-09-25 | Backups model: scheduled workflow + dump inside the container; no cron, no credentials file                                                                                                                                                     | infra doc 09 §9.1a, doc 13 §5; mrmichnik production dump                                               | `docs/phase-0-design.md` 0.9, `decisions.md`                                                         |
| 2026-09-25 | Public-repo scheduled workflows stop after 60 idle days; notifications go to the cron line's last editor                                                                                                                                        | GitHub docs "Events that trigger workflows"                                                            | `docs/phase-0-design.md` 0.9                                                                         |
| 2026-09-25 | Tailwind 4 `outline-none` + `focus-visible:outline-3` yields no ring; `outline-solid` on focus is required                                                                                                                                      | compiled `globals.css` inspected                                                                       | `wiki/gotchas.md`, `focus-ring` utility                                                              |
| 2026-09-25 | pnpm 11 enforces a default minimum release age (lucide-react 1.48.0 refused, 1.47.0 resolved)                                                                                                                                                   | `pnpm add` output, npm publish times                                                                   | `wiki/gotchas.md`, `stack-versions` skill                                                            |
| 2026-09-25 | jsdom 29: no `showModal()`, no `matchMedia`, prefixed `animationend` under React — shims and a timer                                                                                                                                            | `node` probe, failing spec                                                                             | `wiki/gotchas.md`, `test-setup.ts`, `useRipple`                                                      |
| 2026-09-25 | Rubik covers Latin, Cyrillic and Hebrew; self-hosting via fontsource avoids a build-time font fetch                                                                                                                                             | Google Fonts CSS subsets, npm                                                                          | `wiki/ui-design-system.md`, `decisions.md`                                                           |
| 2026-09-25 | Real-message intl tests (`onError` throws) catch missing keys; axe in jsdom needs contrast off                                                                                                                                                  | unit suite, 141 tests                                                                                  | `testing` skill, `wiki/testing.md`, `src/test/`                                                      |
| 2026-09-25 | The host `apps/web/.next` is root-owned; production builds run inside the container                                                                                                                                                             | `ls -ld`, `docker compose run … pnpm run build`                                                        | `wiki/gotchas.md`                                                                                    |
| 2026-09-26 | Next 16's dev cross-origin block checks `Origin`/`Referer` against `allowedDevOrigins` plus the server's own `-H` hostname, never the request `Host`; requests without `Origin` pass, so only the HMR socket and CORS fetches need the hostname | `next/dist/server/lib/router-utils/block-cross-site-dev.js` (16.2.10)                                  | `local-stack` skill, `wiki/deployment.md`                                                            |
| 2026-09-26 | Docker Desktop down on WSL2 (after a Windows reboot) shows as a missing `/var/run/docker.sock` and `docker: unknown command: docker compose` — Desktop mounts the Compose plugin                                                                | this session                                                                                           | `wiki/gotchas.md`, `local-stack` skill                                                               |
| 2026-09-26 | The API could not authenticate after a MySQL restart until some other client had logged in: `caching_sha2_password` full auth over plain TCP needs the RSA key, which the driver fetches only with `allowPublicKeyRetrieval=true`               | `FLUSH PRIVILEGES` + API recreate: 503 for 70 s, 300 aborted connects; with the option healthy in 10 s | `docker-compose.yml`, `wiki/gotchas.md`, `local-stack` and `stack-versions` skills                   |
| 2026-09-26 | The owner keeps myfinpro's push-to-`main` production trigger for the JS projects; dispatch-only is mrmichnik's rule, not a cross-project one — the 2026-09-24 learning is narrowed                                                              | session 2026-09-26                                                                                     | `wiki/decisions.md`, `.claude/rules/infra.md`, `devops` agent, `wiki/infra-context.md`, infra doc 13 |
| 2026-09-26 | myfinpro's overlay PR #51 and backups PR #53 both merged on 2026-09-24; today's notes and infra doc 13 said otherwise — read `gh pr view --json state,mergedAt` before repeating a PR's state                                                   | `gh pr view 51 / 53 -R Aleksei-Michnik/myfinpro`                                                       | infra docs, `wiki/infra-context.md`, home instructions                                               |
| 2026-09-26 | The shared server pulls images anonymously (myfinpro's `deploy.sh` never logs in to GHCR), so this project's packages must be made public after the first build                                                                                 | `~/myfinpro/scripts/deploy.sh`, `deploy-staging.yml`                                                   | `docs/phase-0-design.md` 0.7                                                                         |
| 2026-09-26 | Renamed the toolkit to Mdocker across infra, mrmichnik, myfinpro and this repo; the live switch moves running containers with `docker network connect` before the old network is removed                                                        | this session                                                                                           | compose, `next.config.ts`, `.env.example`, README, skills, wiki                                      |
